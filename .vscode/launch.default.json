{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "AP_ST",
            "cwd": "${workspaceFolder}",
            "executable": "${config:wscript.elf_file_path}",
            "liveWatch": {
                "enabled": true,
                "samplesPerSecond": 4
            },
            "request": "launch",
            "type": "cortex-debug",
            "servertype": "openocd",
            "configFiles": [
                "${workspaceFolder}/build/${config:wscript.board}/openocd.cfg"
            ]
        }
    ]
}
